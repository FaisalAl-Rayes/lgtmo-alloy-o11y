apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

nameSuffix: -stage

resources:
  - ../../base
  - loki-nodeport.yaml

commonLabels:
  environment: stage

patches:
  - path: name-suffix.yaml

