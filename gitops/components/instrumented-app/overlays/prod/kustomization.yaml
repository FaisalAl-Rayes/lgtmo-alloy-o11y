apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: apps-prod

resources:
  - ../../base
  - namespace.yaml

commonLabels:
  environment: prod

patches:
  - path: env-patch.yaml
  - path: ingress-patch.yaml

