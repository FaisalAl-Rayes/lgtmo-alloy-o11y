apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: instrumented-app-ns

resources:
  - ../../base
  - namespace.yaml

commonLabels:
  environment: prod

patches:
  - path: patches/app-deployment-patch.yaml
  - path: env-patch.yaml
  - path: ingress-patch.yaml

