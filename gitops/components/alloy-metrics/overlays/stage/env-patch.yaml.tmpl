apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: alloy-metrics
  namespace: alloy-system
spec:
  template:
    spec:
      containers:
        - name: alloy
          env:
            - name: MIMIR_ENDPOINT
              value: "MIMIR_ENDPOINT_PLACEHOLDER"
            - name: ENVIRONMENT
              value: "stage"

