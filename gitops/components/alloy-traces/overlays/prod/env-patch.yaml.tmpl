apiVersion: apps/v1
kind: Deployment
metadata:
  name: alloy-traces
  namespace: alloy-system
spec:
  template:
    spec:
      containers:
        - name: alloy
          env:
            - name: TEMPO_ENDPOINT
              value: "TEMPO_ENDPOINT_PLACEHOLDER"
            - name: ENVIRONMENT
              value: "prod"

