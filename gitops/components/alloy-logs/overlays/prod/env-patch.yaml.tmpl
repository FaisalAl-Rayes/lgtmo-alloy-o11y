apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: alloy-logs
  namespace: alloy-system
spec:
  template:
    spec:
      containers:
        - name: alloy
          env:
            - name: LOKI_ENDPOINT
              value: "LOKI_ENDPOINT_PLACEHOLDER"
            - name: ENVIRONMENT
              value: "prod"

